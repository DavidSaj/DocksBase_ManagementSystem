import { SecHdr, Empty } from '../shared.jsx';

export default function RequestsInbox() {
  return (
    <div>
      <SecHdr title="Requests" />
      <Empty title="Requests" subtitle="Coming in a follow-up task." />
    </div>
  );
}
