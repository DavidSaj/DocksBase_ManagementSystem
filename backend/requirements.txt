django>=6.0,<7.0
djangorestframework>=3.17,<4.0
djangorestframework-simplejwt>=5.5,<6.0
django-cors-headers>=4.9,<5.0
django-filter>=25.0,<26.0
psycopg2-binary>=2.9,<3.0
gunicorn>=23.0,<24.0
dj-database-url>=2.0,<3.0
dropbox-sign>=1.0,<2.0
pyjwt[crypto]>=2.8,<3.0  # RSA signing for DocuSign JWT Grant
django-storages[s3]>=1.14,<2.0
boto3>=1.43,<2.0
botocore>=1.43,<2.0
s3transfer>=0.17,<1.0
stripe==15.1.0
weasyprint
django-anymail[resend]>=12.0,<13.0
django-csp>=3.8,<4.0
icalendar>=5.0,<6.0

python-dotenv>=1.0,<2.0

# ERP track dependencies
celery[redis]>=5.3
redis>=5.0
django-celery-beat>=2.6,<3.0
django-model-utils>=4.3
requests>=2.31
python-dateutil>=2.8

# SMS providers (used by apps/berths/sms_service.py). Each is imported lazily
# inside its provider branch; only the one matching marina.sms_provider is
# actually loaded at runtime.
twilio>=9.0,<10.0
vonage>=3.14,<4.0
messagebird>=2.0,<3.0

# Used by apps.accounts.encryption for at-rest credential encryption (Fernet).
# Already a transitive dep of several Django packages; pinned explicitly so the
# encryption code never breaks if the transitive chain changes.
cryptography>=42.0
